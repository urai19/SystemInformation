import { Event, Options, Transport, Payload, PayloadOptions, Response, Retry } from '@amplitude/types';
export declare class BaseRetryHandler implements Retry {
    protected readonly _apiKey: string;
    protected readonly _options: Options;
    protected readonly _transport: Transport;
    constructor(apiKey: string, options?: Partial<Options>);
    /**
     * @inheritDoc
     */
    sendEventsWithRetry(events: readonly Event[]): Promise<Response>;
    protected _getPayloadOptions(): {
        options?: PayloadOptions;
    };
    protected _getPayload(events: readonly Event[]): Payload;
}
//# sourceMappingURL=baseRetry.d.ts.map